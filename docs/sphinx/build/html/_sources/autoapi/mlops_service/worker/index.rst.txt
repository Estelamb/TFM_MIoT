mlops_service.worker
====================

.. py:module:: mlops_service.worker

.. autoapi-nested-parse::

   ARQ worker for compilation-service.

   Defines job functions for:
     - train_job:   runs YOLO training subprocess, uploads best.pt to MinIO
     - compile_job: runs hardware-specific compiler, uploads artefact to MinIO

   Both jobs update the model status in ai-service via gRPC on completion.

   The worker runs inside the same Docker container as the gRPC server but as
   a separate process (started via arq app.worker.WorkerSettings).



Attributes
----------

.. autoapisummary::

   mlops_service.worker.logger


Classes
-------

.. autoapisummary::

   mlops_service.worker.WorkerSettings


Functions
---------

.. autoapisummary::

   mlops_service.worker.check_cancellation
   mlops_service.worker.train_job
   mlops_service.worker.compile_job


Module Contents
---------------

.. py:data:: logger

.. py:function:: check_cancellation(redis, cancel_key, process)
   :async:


.. py:function:: train_job(ctx, *, model_id, name, dataset_id, dataset_key, base_architecture, epochs, input_size, gpu_percent, device)
   :async:


.. py:function:: compile_job(ctx, *, model_id, source_key, hardware_type, num_classes, class_names, dataset_id, dataset_key, base_architecture, input_size)
   :async:


.. py:class:: WorkerSettings

   .. py:attribute:: functions


   .. py:attribute:: redis_settings


   .. py:attribute:: queue_name
      :value: 'mlops_queue'



   .. py:attribute:: max_jobs
      :value: 1



   .. py:attribute:: job_timeout
      :value: 7200



   .. py:attribute:: keep_result
      :value: 3600



   .. py:method:: on_startup(ctx)
      :staticmethod:

      :async:


      Initialise shared resources once at worker startup.



   .. py:method:: on_shutdown(ctx)
      :staticmethod:

      :async:



