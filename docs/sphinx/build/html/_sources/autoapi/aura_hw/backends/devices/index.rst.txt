aura_hw.backends.devices
========================

.. py:module:: aura_hw.backends.devices

.. autoapi-nested-parse::

   Device backends for AURA HAL.

   Subpackages
   -----------
   camera
       Backends for image sensors (USB, CSI, IMX500).
   sensor
       Backends for environmental and telemetry sensors (I2C, SPI, UART).



Submodules
----------

.. toctree::
   :maxdepth: 1

   /autoapi/aura_hw/backends/devices/actuator/index
   /autoapi/aura_hw/backends/devices/base/index
   /autoapi/aura_hw/backends/devices/camera/index
   /autoapi/aura_hw/backends/devices/sensor/index


Classes
-------

.. autoapisummary::

   aura_hw.backends.devices.DeviceBackend


Package Contents
----------------

.. py:class:: DeviceBackend(component_id: str)

   Bases: :py:obj:`abc.ABC`


   Lifecycle interface for a single physical device.

   Parameters
   ----------
   component_id:
       The ``id`` field from ``components_config.yaml`` for this device
       (e.g. ``"camera_0"``, ``"env_sensor_0"``).


   .. py:property:: component_id
      :type: str


      Unique component identifier from the config.


   .. py:property:: device_type
      :type: str

      :abstractmethod:


      Device category string.

      :returns: One of ``"camera"``, ``"sensor"``, ``"gpio"``.


   .. py:property:: driver
      :type: str

      :abstractmethod:


      Driver / backend implementation name.

      :returns: A short lowercase identifier such as ``"opencv"``,
                ``"libcamera"``, ``"imx500"``, ``"bme280"``.


   .. py:method:: open(params: dict) -> None
      :abstractmethod:


      Initialise the device with the given configuration parameters.

      :param params: Dict from the ``params`` block in
                     ``components_config.yaml`` for this component.

      :raises RuntimeError: If the required SDK/driver is not available.
      :raises OSError: If the device cannot be opened (e.g. wrong index).



   .. py:method:: close() -> None
      :abstractmethod:


      Release all resources held by this device.

      Safe to call even if :meth:`open` has not been called (no-op).



   .. py:method:: read() -> Any
      :abstractmethod:


      Read the latest data from the device.

      :returns:

                * Camera → ``numpy.ndarray`` (BGR frame)
                * Sensor → ``dict`` with measurement values + units
      :rtype: Depends on the subclass



   .. py:method:: info() -> dict
      :abstractmethod:


      Return a dict describing this device's current state.

      :returns:

                * ``component_id`` — same as :attr:`component_id`
                * ``device_type``  — same as :attr:`device_type`
                * ``driver``       — same as :attr:`driver`
                * ``status``       — ``"open"`` or ``"closed"``
      :rtype: A dict with at minimum



