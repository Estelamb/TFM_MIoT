aura_hw.backends.devices.actuator.relay
=======================================

.. py:module:: aura_hw.backends.devices.actuator.relay


Attributes
----------

.. autoapisummary::

   aura_hw.backends.devices.actuator.relay.logger


Classes
-------

.. autoapisummary::

   aura_hw.backends.devices.actuator.relay.GeneralRelayActuator


Module Contents
---------------

.. py:data:: logger

.. py:class:: GeneralRelayActuator(component_id: str, driver: str)

   Bases: :py:obj:`aura_hw.backends.devices.actuator.base.ActuatorBackend`


   General Relay Actuator library/wrapper.
   Dynamically loads custom relay actuator from hardware/actuators/relay/<driver>/library.py.


   .. py:property:: device_type
      :type: str


      Device category string.

      :returns: One of ``"camera"``, ``"sensor"``, ``"gpio"``.


   .. py:property:: driver
      :type: str


      Driver / backend implementation name.

      :returns: A short lowercase identifier such as ``"opencv"``,
                ``"libcamera"``, ``"imx500"``, ``"bme280"``.


   .. py:method:: open(params: dict) -> None

      Initialise the device with the given configuration parameters.

      :param params: Dict from the ``params`` block in
                     ``components_config.yaml`` for this component.

      :raises RuntimeError: If the required SDK/driver is not available.
      :raises OSError: If the device cannot be opened (e.g. wrong index).



   .. py:method:: close() -> None

      Release all resources held by this device.

      Safe to call even if :meth:`open` has not been called (no-op).



   .. py:method:: write(value: Any) -> None

      Set or trigger the actuator state/action.

      :param value: The state/command value (e.g. RGB color tuple for LED, bool for relay/buzzer).

      :raises RuntimeError: If the actuator is not open.
      :raises IOError: If writing to the actuator fails.



   .. py:method:: info() -> dict

      Return a dict describing this device's current state.

      :returns:

                * ``component_id`` — same as :attr:`component_id`
                * ``device_type``  — same as :attr:`device_type`
                * ``driver``       — same as :attr:`driver`
                * ``status``       — ``"open"`` or ``"closed"``
      :rtype: A dict with at minimum



