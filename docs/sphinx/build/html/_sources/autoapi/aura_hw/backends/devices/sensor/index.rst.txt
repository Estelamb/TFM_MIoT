aura_hw.backends.devices.sensor
===============================

.. py:module:: aura_hw.backends.devices.sensor

.. autoapi-nested-parse::

   Sensor device backends for AURA HAL.

   All sensor backends are loaded dynamically from the ``hardware/``
   directory via :class:`GeneralSensorBackend`.



Submodules
----------

.. toctree::
   :maxdepth: 1

   /autoapi/aura_hw/backends/devices/sensor/base/index
   /autoapi/aura_hw/backends/devices/sensor/distance/index
   /autoapi/aura_hw/backends/devices/sensor/general/index
   /autoapi/aura_hw/backends/devices/sensor/imu/index
   /autoapi/aura_hw/backends/devices/sensor/temperature/index


Classes
-------

.. autoapisummary::

   aura_hw.backends.devices.sensor.SensorBackend
   aura_hw.backends.devices.sensor.GeneralSensorBackend


Package Contents
----------------

.. py:class:: SensorBackend(component_id: str)

   Bases: :py:obj:`aura_hw.backends.devices.base.DeviceBackend`


   Specialised device backend for measurement sensors.

   Adds the :meth:`measure` method, which returns a dict of labelled
   readings with their physical units.


   .. py:property:: device_type
      :type: str


      Device category string.

      :returns: One of ``"camera"``, ``"sensor"``, ``"gpio"``.


   .. py:method:: measure() -> dict
      :abstractmethod:


      Read all measurements from the sensor.

      :returns:

                A dict mapping measurement names to values, for example::

                    {
                        "temperature_c":  23.4,
                        "humidity_pct":   55.1,
                        "pressure_hpa": 1013.2,
                    }

      :raises RuntimeError: If the sensor has not been opened.
      :raises IOError: If the measurement fails (e.g. I2C bus error).



   .. py:method:: read() -> dict

      Alias for :meth:`measure` — satisfies the :class:`DeviceBackend` contract.



.. py:class:: GeneralSensorBackend(component_id: str, driver: str)

   Bases: :py:obj:`aura_hw.backends.devices.sensor.base.SensorBackend`


   General Sensor library/wrapper.
   - If driver is 'bme280', delegates to the built-in BME280Backend.
   - Otherwise, dynamically loads custom sensor from hardware/sensors/sensor/<driver>/library.py.


   .. py:property:: driver
      :type: str


      Driver / backend implementation name.

      :returns: A short lowercase identifier such as ``"opencv"``,
                ``"libcamera"``, ``"imx500"``, ``"bme280"``.


   .. py:method:: open(params: dict) -> None

      Initialise the device with the given configuration parameters.

      :param params: Dict from the ``params`` block in
                     ``components_config.yaml`` for this component.

      :raises RuntimeError: If the required SDK/driver is not available.
      :raises OSError: If the device cannot be opened (e.g. wrong index).



   .. py:method:: close() -> None

      Release all resources held by this device.

      Safe to call even if :meth:`open` has not been called (no-op).



   .. py:method:: measure() -> dict

      Read all measurements from the sensor.

      :returns:

                A dict mapping measurement names to values, for example::

                    {
                        "temperature_c":  23.4,
                        "humidity_pct":   55.1,
                        "pressure_hpa": 1013.2,
                    }

      :raises RuntimeError: If the sensor has not been opened.
      :raises IOError: If the measurement fails (e.g. I2C bus error).



   .. py:method:: info() -> dict

      Return a dict describing this device's current state.

      :returns:

                * ``component_id`` — same as :attr:`component_id`
                * ``device_type``  — same as :attr:`device_type`
                * ``driver``       — same as :attr:`driver`
                * ``status``       — ``"open"`` or ``"closed"``
      :rtype: A dict with at minimum



